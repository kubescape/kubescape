apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: empty-resourcenames-pod-editor
  namespace: default
rules:
- apiGroups: [""]
  resources: ["pods"]
  resourceNames: []
  verbs: ["update"]
