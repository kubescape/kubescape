apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kube-system-pod-patcher
  namespace: kube-system
rules:
- apiGroups: ["", "*"]
  resources: ["pods", "*"]
  verbs: ["patch", "*"]
