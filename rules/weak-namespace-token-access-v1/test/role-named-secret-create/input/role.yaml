apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: named-secret-creator
  namespace: build
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["forged-token"]
  verbs: ["create", "list"]
