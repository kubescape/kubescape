apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: empty-resourcenames-secret-lister
  namespace: dev
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: []
  verbs: ["list"]
