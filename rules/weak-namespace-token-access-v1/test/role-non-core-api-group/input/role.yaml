apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: external-secret-reader
  namespace: dev
rules:
- apiGroups: ["external-secrets.io"]
  resources: ["secrets"]
  verbs: ["get", "list"]
