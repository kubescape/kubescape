apiVersion: v1
kind: Pod
metadata:
  name: cni-installer
  namespace: kube-system
spec:
  hostNetwork: true
  hostPID: true
  containers:
  - name: installer
    image: registry.example.com/cni-installer:0.9.0
    securityContext:
      privileged: true
