apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: named-secret-applier
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["sa-token"]
  verbs: ["create", "patch"]
