apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: empty-resourcenames-secret-minter
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: []
  verbs: ["create", "get"]
