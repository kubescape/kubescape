apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: unrestricted-create-named-get
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["create"]
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["sa-token"]
  verbs: ["get"]
