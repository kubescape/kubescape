apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: split-secret-minter-crb
subjects:
- kind: ServiceAccount
  name: split-sa
  namespace: ci
roleRef:
  kind: ClusterRole
  name: split-secret-minter
  apiGroup: rbac.authorization.k8s.io
