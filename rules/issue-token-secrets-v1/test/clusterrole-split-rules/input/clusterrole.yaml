apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: split-secret-minter
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["create"]
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get"]
