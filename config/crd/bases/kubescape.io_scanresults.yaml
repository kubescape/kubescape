---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: scanresults.kubescape.io
spec:
  group: kubescape.io
  names:
    kind: ScanResult
    listKind: ScanResultList
    plural: scanresults
    singular: scanresult
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: |-
          ScanResult is the Schema for the scanresults API.
          It stores the summarized outcome of a completed ScanRequest.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScanResultSpec defines the inputs that produced this result.
            properties:
              scanRequestRef:
                description: ScanRequestRef points to the ScanRequest that generated
                  this result.
                type: string
            required:
            - scanRequestRef
            type: object
          status:
            description: ScanResultStatus defines the observed findings.
            properties:
              completionTime:
                description: CompletionTime is when the scan finished.
                format: date-time
                type: string
              failedControls:
                description: FailedControls is the number of controls that failed.
                type: integer
              severitySummary:
                additionalProperties:
                  type: integer
                description: SeveritySummary breaks down the findings by severity.
                type: object
              totalControls:
                description: TotalControls is the number of controls evaluated.
                type: integer
            required:
            - failedControls
            - totalControls
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
