---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: scanpolicies.kubescape.io
spec:
  group: kubescape.io
  names:
    kind: ScanPolicy
    listKind: ScanPolicyList
    plural: scanpolicies
    singular: scanpolicy
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: |-
          ScanPolicy is the Schema for the scanpolicies API.
          It defines cluster-wide defaults and enforcement rules for scans.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScanPolicySpec defines the default behaviors.
            properties:
              defaultFrameworks:
                description: DefaultFrameworks defines the fallback frameworks if
                  a ScanRequest omits them.
                items:
                  type: string
                type: array
              maxConcurrentScans:
                default: 3
                description: MaxConcurrentScans prevents overloading the cluster.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
