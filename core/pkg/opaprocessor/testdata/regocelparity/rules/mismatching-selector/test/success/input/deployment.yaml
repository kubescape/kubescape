apiVersion: apps/v1
kind: Deployment
metadata:
  name: matching-selector-deployment
spec:
  selector:
    matchLabels:
      app: api
      tier: backend
  template:
    metadata:
      labels:
        app: api
        tier: backend
        track: stable
    spec:
      containers:
      - name: app
        image: nginx
