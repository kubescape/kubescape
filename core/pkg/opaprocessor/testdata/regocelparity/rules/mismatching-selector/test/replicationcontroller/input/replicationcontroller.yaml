apiVersion: v1
kind: ReplicationController
metadata:
  name: mismatching-selector-rc
spec:
  selector:
    app: api
    tier: backend
  template:
    metadata:
      labels:
        app: api
        tier: frontend
    spec:
      containers:
      - name: app
        image: nginx
