apiVersion: apps/v1
kind: Deployment
metadata:
  name: mismatching-selector-deployment
spec:
  selector:
    matchLabels:
      app: api
      tier: backend
  template:
    metadata:
      labels:
        app: api
        tier: frontend
    spec:
      containers:
      - name: app
        image: nginx
