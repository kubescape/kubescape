apiVersion: apps/v1
kind: Deployment
metadata:
  name: mismatching-selector-missing-template-metadata
spec:
  selector:
    matchLabels:
      app: api
  template:
    spec:
      containers:
      - name: app
        image: nginx
